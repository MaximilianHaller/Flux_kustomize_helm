apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: solr
  namespace: flux-system
spec:
  interval: 1m
  chart:
    spec:
      chart: solr
      version: '0.8.0'
      sourceRef:
        kind: HelmRepository
        name: solr
        namespace: system
      interval: 1m
  values:
    replicaCount: 1